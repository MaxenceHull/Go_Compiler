//Assigning function call that results in an int to a string variable. Invalid
package main;
func main(a, b, z int, w string, x, y float64) int {
  return 1
}

func doom() {
  var xa string;
  var xb int;
  var xc float64;
  xa = main(xb, xb, xb, xa, xc, xc);
}
